node_modules/
.env
.env.local
dist/
.vercel/
.astro/
# Die Generator-Skills unter .claude/skills/ sind Projektinhalt (sie tragen den
# nRLP-/Lehrmittel-Crosswalk und die Didaktik-Regeln) und werden versioniert.
# Nur die maschinenlokalen Einstellungen bleiben draussen.
.claude/settings.local.json
CLAUDE.md
abu-plattform-supabase-handoff.md
supabase/setup_full.sql
index.html
# …but the nRLP static apps need their entry points committed/deployed (Vercel)
!public/nrlp/index.html
!public/nrlp/prompt-builder/index.html

# Lehrmittel-Quelltexte (urheberrechtlich, nicht-öffentlich, nicht deployt)
# Wildcard deckt auch die abgelegten Vorgängerausgaben ab (_lehrmittel_2025/ usw.)
material/_lehrmittel*/

# Video-Deck (HyperFrames-Projekt unter videos/deck-uebersicht/).
# Reines Produktionsmaterial: Renders, Audio-Samples, Frame-Packets, Waveform-Cache
# und Thumbnails summieren sich auf ~175 MB und entstehen jederzeit neu aus BRIEF.md
# und STORYBOARD.md. Sie gehoeren nicht in die Historie der Plattform.
videos/

# Chromium/Crashpad-Rauschen aus dem Browser-Pane
debug.log

# Schreibrechte-Proben, die Tools in Skill-Ordnern hinterlassen
.__wtest*
.__probe

# Fremdmaterial: SCORM-Pakete von Pascal Rusch (Mail 17.09.2026), Analysegrundlage
# fuer einen eigenen SCORM-Export. Nicht unser Code, nicht deployen.
_scorm-rusch/

# SCORM-Pakete entstehen jederzeit neu aus scripts/build-scorm.mjs; das ZIP ist
# ein binaeres Build-Artefakt und gehoert nicht in die Historie.
scorm/generated/
